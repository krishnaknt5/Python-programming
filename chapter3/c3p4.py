# 4. Replace the double space from problem 3 with single spaces.
text = "kk is a good  boy"
print(text.replace("  ", " "))
# This will replace the double spaces with single spaces and print the modified string.
#the string text has not changed, it is just gave a new output that has changed.